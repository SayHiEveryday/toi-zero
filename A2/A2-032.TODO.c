#include <stdio.h>

int main() {
    int N, M, score = 0;
    scanf("%d %d", &N, &M);
    int PokeAmount;
    scanf("%d", &PokeAmount);

    int PokeX[500], PokeY[500];

    for (int i = 0; i < PokeAmount; i++) {
        scanf("%d %d", &PokeX[i], &PokeY[i]);
    }

    for (int i = 0; i < N; i++) {
        for (int j = 0; j < M; j++) {

        }
    }

}
